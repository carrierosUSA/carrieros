import PageHeader from "@/components/PageHeader";

export default function SettingsPage() {
  return (
    <PageHeader
      title="Settings"
      subtitle="Workspace, users, roles, permissions, and preferences."
    />
  );
}
