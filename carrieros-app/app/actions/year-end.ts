"use server";import{requireDocumentAuth}from"@/lib/auth/supabase-server";import{YearEndRepository,type YearEndMonth}from"@/lib/year-end/records";export async function getYearEndRecordsAction(year:number):Promise<{ok:true;months:YearEndMonth[]}|{ok:false;error:string}>{if(!Number.isInteger(year)||year<2000||year>new Date().getUTCFullYear()+1)return{ok:false,error:"Choose a valid reporting year."};try{const a=await requireDocumentAuth();if(!["super_admin","owner","accounting"].includes(a.businessRole))return{ok:false,error:"Year-end records are restricted to owners and accounting."};return{ok:true,months:await new YearEndRepository().list(a.accessToken,year)}}catch{return{ok:false,error:"Verified year-end records are unavailable."}}}
