import assert from"node:assert/strict";import{readFileSync}from"node:fs";import test from"node:test";const script=readFileSync("scripts/development-target-preflight.mjs","utf8"),manifest=readFileSync("scripts/migration-manifest.mjs","utf8"),env=readFileSync(".env.example","utf8"),guide=readFileSync("docs/testing-handoff.md","utf8"),pkg=JSON.parse(readFileSync("package.json","utf8"))as{scripts:Record<string,string>};
test("development target requires explicit environment and exact project reference",()=>{assert.match(script,/TRANSPO_ENVIRONMENT/);assert.match(script,/must equal development/);assert.match(script,/TRANSPO_EXPECTED_SUPABASE_PROJECT_REF/);assert.match(script,/expected!==actual/);assert.match(env,/TRANSPO_ENVIRONMENT=development/)});
test("preflight rejects placeholders public secrets and credential collisions",()=>{assert.match(script,/YOUR_\|example\|placeholder/);assert.match(script,/NEXT_PUBLIC_\$\{name\}/);assert.match(script,/must not be identical/);assert.doesNotMatch(script,/console\.(?:log|error)\([^)]*process\.env/)});
test("migration manifest is deterministic read only and checksummed",()=>{assert.match(manifest,/\.sort\(\)/);assert.match(manifest,/sha256/);assert.match(manifest,/No database connection or SQL execution occurred/);assert.doesNotMatch(manifest,/supabase db push|psql|execSync|spawn/)});
test("controlled handoff runs target guard before any separately approved migration",()=>{assert.match(guide,/npm run preflight:development/);assert.match(guide,/npm run migration:manifest/);assert.match(guide,/separate human-approved database action/);assert.match(pkg.scripts["verify:development-target"],/preflight:development.*migration:manifest.*verify:testing/)});
