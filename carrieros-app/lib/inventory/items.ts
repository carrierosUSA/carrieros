import"server-only";import{getSupabaseAuthenticatedUserClient}from"@/lib/supabase/server";export type InventoryItem={id:string;name:string;sku?:string;category:string;unit:string;quantity:number;reorderLevel:number;location?:string;status:"active"|"inactive";factualNote:string};type Row=Record<string,unknown>;const t=(v:unknown)=>typeof v==="string"?v:"",n=(v:unknown)=>Number.isFinite(Number(v))?Number(v):0;export class InventoryRepository{async list(token:string):Promise<InventoryItem[]>{const db=getSupabaseAuthenticatedUserClient(token),r=await db.rpc("list_verified_inventory_items");if(r.error)throw new Error("Inventory unavailable");return(Array.isArray(r.data)?r.data:[]).flatMap(v=>{if(!v||typeof v!=="object")return[];const x=v as Row,id=t(x.id),status=t(x.status);return id&&(status==="active"||status==="inactive")?[{id,name:t(x.item_name),sku:t(x.sku)||undefined,category:t(x.category),unit:t(x.unit_of_measure),quantity:n(x.quantity_on_hand),reorderLevel:n(x.reorder_level),location:t(x.storage_location)||undefined,status,factualNote:t(x.factual_note)}]:[]})}}
