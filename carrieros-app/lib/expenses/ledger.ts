import"server-only";import{getSupabaseAuthenticatedUserClient}from"@/lib/supabase/server";export type Expense={id:string;assetId?:string;unitNumber?:string;category:string;incurredOn:string;amountCents:number;vendor?:string;sourceReference?:string;factualNote:string;status:"active"|"voided"};type R=Record<string,unknown>;const s=(v:unknown)=>typeof v==="string"?v:"";export class ExpenseRepository{async list(token:string):Promise<Expense[]>{const db=getSupabaseAuthenticatedUserClient(token),r=await db.rpc("list_verified_operating_expenses");if(r.error)throw new Error("Expenses unavailable");return(Array.isArray(r.data)?r.data:[]).flatMap(v=>{if(!v||typeof v!=="object")return[];const x=v as R,id=s(x.id),status=s(x.status);return id&&(status==="active"||status==="voided")?[{id,assetId:s(x.asset_id)||undefined,unitNumber:s(x.unit_number)||undefined,category:s(x.category),incurredOn:s(x.incurred_on),amountCents:Number(x.amount_cents)||0,vendor:s(x.vendor)||undefined,sourceReference:s(x.source_reference)||undefined,factualNote:s(x.factual_note),status}]:[]})}}
